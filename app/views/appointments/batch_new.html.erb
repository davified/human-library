<h1>Create event schedule</h1>
<p>
    Date:
    <%= @event.date %>
</p>
<p>
    Venue:
    <%= @event.venue %>
</p>
<p>
    Start time:
    <%= @event.start_time.strftime("%I:%M%p") %>
</p>
<table border='1'>
    <tr>
        <td></td>
        <% @slots.each do |slot| %>
        <td>
            <%= slot.start_time.strftime("%I:%M%p") %>
        </td>
        <% end %>
    </tr>
    <% @books.each do |book| %>
    <tr>
        <td>
            <%= select_tag(:book_id, options_for_select(@books_name_only))%>
            <!-- book.user.name -->
            <!--  reference for dropdown options -->
            <!-- <div class="field"> -->
            <!-- f.label :book_id %> -->
            <!-- f.select :book_id, Book.all.collect { |book| [ book.user.name, book.user.id ] } %> -->
            <!-- </div> -->
        </td>
        <% @slots.each do |slot| %>
        <td>
            <%= select_tag(:boolean, options_for_select([['Available', true], ['Not available', false]])) %>
        </td>
        <% end %>
        <% end %>
    </tr>
</table>
<br>
<button type="button" name="button" class='btn'>Create Schedule</button>
